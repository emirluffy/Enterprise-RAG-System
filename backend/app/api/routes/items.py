"""
Disabled - Item model not used in Enterprise RAG System
"""
from fastapi import APIRouter

router = APIRouter(prefix="/items", tags=["items"])

# All item routes disabled - not part of Enterprise RAG system
# This module exists for template compatibility only
